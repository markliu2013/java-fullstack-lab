## 1. 为什么需要并发？
- 异步执行（OrderAsyncPerformance.java）
- 分布式计算（MultiThreadComparison.java）

## 2. 线程怎么正确运行？
    Thread.start() vs run()
    线程状态（NEW / RUNNABLE / BLOCKED / WAITING / TIMED_WAITING / TERMINATED）

## 3. 多个线程一起工作，会不会出问题？（真正的核心问题开始）（用三段代码说明）
- 原子性（AtomicityDemo.java，StockOversellDemo.java）
- 可见性（VisibilityDemo.java）
- 有序性（ReorderingLitmusTest.java，CpuReorderDemo.java）

## 4. 怎么解决这些问题？（核心重点）
- happens-before 规则
- volatile（可见性、有序性）
- 加锁（synchronized、ReentrantLock）（三个都解决）
    - 锁的分类
    - 多个线程抢锁，怎么排队？（AQS）
- CAS（Compare And Swap）原子类（AtomicInteger、AtomicReference）
- ThreadLocal

## 5. 线程之间如何协作？

### 5.1 线程为什么需要等待？
   （一个线程暂时没有事情做，应该怎么办？）
   （阻塞、等待、唤醒）
   （生产者和消费者，避免浪费cpu）
- 我只是想暂停一段时间（sleep）
- 我等待某个条件满足（wait / notify）
- 更底层、更灵活的线程阻塞机制（LockSupport）

### 5.2 多个线程如何交换信息？——线程通信
    （多个线程如何共同完成一个复杂任务？）
    （主线程等待多个子任务完成）
    （多个线程如何交换信息？）

- volatile
- wait/notify
- CountDownLatch
- CyclicBarrier
- Semaphore
- LockSupport
- 异步任务编排（Callable、Future、FutureTask、CompletableFuture）

### 5.3 线程如何停止（优雅停止）？
- interrupt()
- isInterrupted()
- interrupted()

## 8. 线程太多怎么办？
- 线程池（ThreadPoolExecutor）

## 9. 集合在并发下安全吗？
- ConcurrentHashMap
- CopyOnWriteArrayList

## 10. 异步任务编排
    - Callable
    - Future
    - FutureTask
    - CompletableFuture



为什么需要并发 → 多线程如何运行 → 多线程有什么问题 → 如何保证线程安全 → 如何进行线程协作 → 如何管理线程资源

线程为什么会“卡死”？死锁
