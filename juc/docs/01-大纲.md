## 1. 为什么需要并发？
- 异步执行
- 分布式计算 (MultiThreadComparison.java)

## 2. 线程怎么正确运行？
    Thread.start() vs run()
    线程状态（NEW / RUNNABLE / BLOCKED / WAITING / TIMED_WAITING / TERMINATED）

## 3. 多个线程一起工作，会不会出问题？（真正的核心问题开始）（用三段代码说明）
- 原子性
- 可见性
- 有序性

## 4. 怎么解决这些问题？（核心重点）
- happens-before 规则
- volatile（可见性、有序性）
- 加锁（synchronized、ReentrantLock）（三个都解决）
    - 锁的分类
    - 多个线程抢锁，怎么排队？（AQS）
- CAS（Compare And Swap）原子类（AtomicInteger、AtomicReference）
- ThreadLocal

## 5. 线程如何“等待与唤醒”？（阻塞模型）
三种机制对比（sleep、wait、park）

## 6. 线程之间如何“协作”？线程之间怎么通信？
- volatile
- wait / notify
- CountDownLatch、CyclicBarrier、Semaphore
- LockSupport

## 7. 线程怎么停止？
- 三个interupt方法

## 8. 线程太多怎么办？
- 线程池（ThreadPoolExecutor）

## 9. 集合在并发下安全吗？
- ConcurrentHashMap
- CopyOnWriteArrayList

## 10. 异步任务编排
    - Callable
    - Future
    - FutureTask
    - CompletableFuture


线程为什么会“卡死”？死锁
